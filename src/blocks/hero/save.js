/**
 * External dependencies
 */
import classnames from 'classnames';

const save = ( props ) => {
	const { className } = props;

	return (
		<div id="unityblocks-hero" className={ classnames( className ) }></div>
	);
};

export default save;
